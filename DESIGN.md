# 华夏疆域 · 历代演变 — 技术方案设计

> 状态：待确认（grilling 讨论已达共识，确认后开工）
> 日期：2026-09-24

## 1. 项目定位

**个人兴趣 + 低调公开**。自己学习把玩为主，部署到 GitHub Pages 谁都能访问，但不做推广、不商用。数据合规压力小（标注来源即可），打磨程度随兴趣迭代。

## 2. 范围

| 维度 | 决策 |
|---|---|
| 朝代 | 第一版六大朝代：**秦、西汉、唐、元、明、清**，每朝一张疆域快照 |
| 事件 | **疆域相关事件优先**（战争/和亲/条约/开拓），每朝 8~12 条，带经纬度可地图定位 |
| 平台 | **桌面优先、移动可用**：桌面做精；手机能打开看图切朝代，侧栏收成抽屉，不专门打磨 |
| 二期候选 | 朝代内多年份细分、分裂期朝代（三国/南北朝/五代）、通史事件标签筛选、今省会城市点 |

## 3. 技术栈

**Vite + 原生 JS + ECharts 5**（无框架）

- ECharts `geo`/`map` 组件渲染疆域 GeoJSON，事件用 `scatter` 系列 + tooltip
- 自定义 HTML 朝代刻度条驱动 `setOption`（不用 ECharts 内置 timeline 组件——样式和过渡更可控）
- 各朝代 GeoJSON **按需 fetch 懒加载**，首屏只拉当前朝代
- 现代轮廓 = 淡色细线 `lines`/`map` 垫底层，默认开、右上角开关

## 4. 数据方案（本项目 80% 工作量）

### 4.1 疆域边界

- **策略：用现成数字化数据 + 每张图标注来源**（GitHub 搜 `historical china geojson`、`CHGIS`、谭其骧）
- 候选仓库：`china-historical-geojson`（谭图数字化）、按朝代+年份分文件的项目（源自哈佛-复旦 CHGIS）
- 每朝选一个代表年份快照（如唐取 748 年前后极盛期），拉回后**逐朝目视校对**边界合理性
- 版权备注：谭图数字化属灰区，个人非商用 + 标注出处风险低；若未来商业化需重新评估

### 4.2 事件数据（自建，JSON 入库）

```jsonc
// data/events/tang.json
[
  {
    "year": 630,            // 负数=公元前
    "yearLabel": "630年",
    "title": "唐灭东突厥",
    "description": "李靖夜袭定襄……此后漠北纳入唐朝势力范围",
    "location": { "name": "阴山", "lng": 111.0, "lat": 41.0 },
    "tag": "war",           // war | treaty | expansion | figure
    "source": "旧唐书/资治通鉴"
  }
]
```

### 4.3 朝代索引（驱动整个应用）

```jsonc
// data/dynasties.json
[
  {
    "id": "tang",
    "name": "唐",
    "period": "618–907",
    "snapshotYear": 748,
    "geojson": "geo/748_tang.json",
    "color": "#B03A2E",
    "summary": "……疆域概述两三句……"
  }
]
```

### 4.4 目录结构

```
dynasty-map/
├── DESIGN.md
├── index.html
├── src/
│   ├── main.js          # 入口：加载数据、装配模块
│   ├── map.js           # ECharts 初始化、朝代切换、事件图层
│   ├── timeline.js      # 刻度条交互（点击/键盘/过渡）
│   └── sidebar.js       # 朝代信息 + 大事年表 + 事件定位
├── data/
│   ├── dynasties.json
│   ├── geo/             # 各朝 GeoJSON（懒加载）
│   └── events/          # 各朝事件
└── public/  (vite 静态资源，含现代轮廓 geojson)
```

## 5. 交互与布局

```
┌──────────────────────────────────────────────┐
│  华夏疆域 · 历代演变            [现代轮廓 ◉] │
├───────────────────────────────┬──────────────┤
│                               │ 唐 618–907   │
│                               │ ──────────── │
│        地 图 区 域             │ 疆域概述……   │
│   （朝代色块 + 现代轮廓垫底）   │              │
│    事件点 ● 可点击弹详情        │ 大事年表     │
│                               │ · 755 安史…  │
│                               │ · 630 灭东突…│
├───────────────────────────────┴──────────────┤
│  秦 ─── 西汉 ───【唐】─── 元 ─── 明 ─── 清   │
└──────────────────────────────────────────────┘
```

- **时间轴**：底部朝代刻度条；点击 / 键盘 ←→ 切换；切换时疆域色块淡入淡出过渡
- **事件流**：侧栏年表点击 → 地图视角飞到事件坐标 + 高亮脉冲点 + 弹出详情卡；地图点反向联动侧栏
- **现代轮廓**：淡色细线垫底（默认开、可隐藏），帮助建立「唐朝原来这么大」的方位感
- **移动端**（≤768px）：侧栏收成底部抽屉，地图全屏，刻度条横向滚动

## 6. 部署

GitHub Pages（复用 banzheshenghuo 账号已跑通的管线）：`vite build` → 推 `dist/` → 开 Pages。纯静态，无服务端。

## 7. 里程碑

| 阶段 | 内容 | 交付 |
|---|---|---|
| M1 骨架 | Vite 工程 + 地图 + 刻度条 + 侧栏 + 示例数据（每朝先 3 条事件、GeoJSON 能拉到几个是几个） | 本地可玩的完整交互 |
| M2 数据 | 六朝 GeoJSON 全部校对入库 + 每朝 8~12 条事件录入（我先起草，你复核） | 内容完整版 |
| M3 上线 | 过渡动画打磨 + 移动抽屉 + 部署 GitHub Pages | 公网可访问 |

## 8. 已决决策清单（grilling 存档）

1. 定位：个人兴趣 + 低调公开
2. 范围：六大朝代起步（秦/西汉/唐/元/明/清）
3. 疆域数据：现成数字化 GeoJSON + 标来源
4. 事件口径：疆域相关优先，每朝 8~12 条带坐标
5. 技术栈：Vite + 原生 JS + ECharts
6. 地图：叠加现代轮廓（默认开可关）
7. 时间轴：朝代刻度条 + 键盘切换 + 过渡动画
8. 适配：桌面优先、移动可用
