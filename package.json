{
  "name": "dynasty-map",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "data": "node tools/build-data.mjs",
    "check": "node tools/validate.mjs && vite build"
  },
  "dependencies": {
    "echarts": "^5.6.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
